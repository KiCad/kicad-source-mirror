WXDIR = $(WXWIN)

all: common.a

include ../libs.win

include makefile.include

common.a: $(OBJECTS) ../libs.win makefile.include
	ar ruv $@ $(OBJECTS)
	ranlib $@

clean:
	rm -f *.bak
	rm -f *.o
	rm -f common.a
